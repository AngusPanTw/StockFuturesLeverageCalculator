<Window x:Class="LeverageCalculator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LeverageCalculator"
        xmlns:viewmodels="clr-namespace:LeverageCalculator.ViewModels"
        xmlns:models="clr-namespace:LeverageCalculator.Models"
        xmlns:converters="clr-namespace:LeverageCalculator.Converters"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModel, IsDesignTimeCreatable=True}"
        Title="投資組合槓桿計算機 v2.0 (WPF)" Height="800" Width="1600">
    <Window.Resources>
        <!-- Global Styles for UI Beautification -->
        <Style TargetType="GroupBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="BorderBrush" Value="#FFCCCCCC"/>
            <Setter Property="Background" Value="#FFFDFDFD"/>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="#FF333333"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Height" Value="26"/>
            <Setter Property="BorderBrush" Value="#FFAAAAAA"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="10,4"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style TargetType="RadioButton">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="RowHeight" Value="32"/>
            <Setter Property="ColumnHeaderHeight" Value="34"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#FFE0E0E0"/>
            <Setter Property="AlternatingRowBackground" Value="#FFF8F8F8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#FFDDDDDD"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="#FFF0F0F0"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="BorderBrush" Value="#FFDDDDDD"/>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="8,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="{TemplateBinding Padding}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                            <ContentPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RightAlignedCellStyle" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Padding" Value="5,0"/>
        </Style>

        <Style x:Key="CenterAlignedCellStyle" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>
    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*" MinWidth="500"/>
            <ColumnDefinition Width="1*" MinWidth="300"/>
        </Grid.ColumnDefinitions>

        <!-- Column 0: General Assets & Stocks -->
        <DockPanel Grid.Column="0" Margin="0,0,10,0">
            <GroupBox Header="總資產" DockPanel.Dock="Top" Padding="5">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="銀行可用現金:" VerticalAlignment="Center" Margin="5"/>
                    <TextBox Grid.Column="1" Text="{Binding BankCash, UpdateSourceTrigger=PropertyChanged, StringFormat=N0}" Margin="5" HorizontalAlignment="Stretch"/>
                    <TextBlock Grid.Row="1" Text="股票交割款(近三日):" VerticalAlignment="Center" Margin="5"/>
                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding StockSettlementAmount, UpdateSourceTrigger=PropertyChanged, StringFormat=N0}" Margin="5" HorizontalAlignment="Stretch"/>
                </Grid>
            </GroupBox>

            <GroupBox Header="股票庫存" Margin="0,10,0,0" Padding="5">
                <DockPanel>                   

                    <GroupBox DockPanel.Dock="Bottom" Header="新增股票" Padding="5">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <!-- Row 0 -->
                            <TextBlock Text="股票名稱:" VerticalAlignment="Center" Margin="2"/>
                            <TextBox Grid.Column="1" Text="{Binding NewStockName, UpdateSourceTrigger=PropertyChanged}" Margin="2"/>
                            
                            <TextBlock Grid.Column="2" Text="持有股數:" VerticalAlignment="Center" Margin="10,2,2,2"/>
                            <TextBox Grid.Column="3" Text="{Binding NewStockShares, StringFormat={}{0:N0}}" Margin="2"/>
                            
                            <!-- Row 1 -->
                            <TextBlock Grid.Row="1" Text="總市值:" VerticalAlignment="Center" Margin="2"/>
                            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding NewStockMarketValue, StringFormat={}{0:N0}}" Margin="2"/>

                            <TextBlock Grid.Row="1" Grid.Column="2" Text="帳面損益:" VerticalAlignment="Center" Margin="10,2,2,2"/>
                            <TextBox Grid.Row="1" Grid.Column="3" Text="{Binding NewStockProfitLoss, StringFormat={}{0:N0}}" Margin="2"/>
                            
                            <!-- Row 2 -->
                            <TextBlock Grid.Row="2" Text="報酬率(%):" VerticalAlignment="Center" Margin="2"/>
                            <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding NewStockProfitLossPercentage}" Margin="2"/>
                            
                            <!-- Row 3 -->
                            <Button Grid.Row="3" Grid.ColumnSpan="4" Content="新增股票" Command="{Binding AddStockCommand}" Margin="5"/>
                        </Grid>
                    </GroupBox>

                    <DataGrid ItemsSource="{Binding Stocks}" AutoGenerateColumns="False" CanUserAddRows="False" ColumnWidth="Auto">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="名稱" Binding="{Binding Name}" ElementStyle="{StaticResource CenterAlignedCellStyle}"/>
                            <DataGridTextColumn Header="股數" Binding="{Binding Shares, StringFormat=N0}" ElementStyle="{StaticResource RightAlignedCellStyle}"/>
                            <DataGridTextColumn Header="總市值" Binding="{Binding MarketValue, StringFormat={}{0:#,##0}}" ElementStyle="{StaticResource RightAlignedCellStyle}"/>
                            <DataGridTemplateColumn Header="損益">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding ProfitLoss, StringFormat={}{0:N0}}" Style="{StaticResource RightAlignedCellStyle}" Foreground="{Binding ProfitLossColor}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                                <DataGridTemplateColumn.CellEditingTemplate>
                                    <DataTemplate>
                                        <TextBox Text="{Binding ProfitLoss, UpdateSourceTrigger=LostFocus}" HorizontalContentAlignment="Right" VerticalContentAlignment="Center"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellEditingTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="報酬率 (%)">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding ProfitLossPercentage100, StringFormat={}{0:N2}%}" Style="{StaticResource RightAlignedCellStyle}" Foreground="{Binding ProfitLossColor}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                                <DataGridTemplateColumn.CellEditingTemplate>
                                    <DataTemplate>
                                        <TextBox Text="{Binding ProfitLossPercentage100, UpdateSourceTrigger=LostFocus}" HorizontalContentAlignment="Right" VerticalContentAlignment="Center"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellEditingTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Width="80">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="刪除" Command="{Binding DataContext.DeleteStockCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}" Margin="0" Height="24" HorizontalAlignment="Center" Width="60"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </GroupBox>
        </DockPanel>

        <!-- Column 1: Results (Moved from Column 2) -->
        <GroupBox Grid.Column="1" Header="📊 最終結算報告" Margin="10,0,0,0" Padding="10" FontWeight="Bold">
            <StackPanel>
                <TextBlock Text="[資產分佈]" Margin="0,5" FontSize="14"/>
                <Separator/>
                <Grid Margin="5">
                    <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions>
                    <TextBlock Text="股票總市值:" />
                    <TextBlock Grid.Column="1" Text="{Binding TotalStockValue, StringFormat={}{0:N0} 元}" HorizontalAlignment="Right"/>
                </Grid>
                <Grid Margin="5">
                    <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions>
                    <TextBlock Text="銀行總存款:" />
                    <TextBlock Grid.Column="1" Text="{Binding BankCash, StringFormat={}{0:N0} 元}" HorizontalAlignment="Right"/>
                </Grid>
                <Grid Margin="5">
                    <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions>
                    <TextBlock Text="股票交割款:" />
                    <TextBlock Grid.Column="1" Text="{Binding StockSettlementAmount, StringFormat={}{0:N0} 元}" HorizontalAlignment="Right"/>
                </Grid>

                <TextBlock Text="[曝險與槓桿]" Margin="0,20,0,5" FontSize="14"/>
                <Separator/>
                <Grid Margin="5">
                    <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions>
                    <TextBlock Text="▶ 總曝險部位 (分子):" />
                    <TextBlock Grid.Column="1" Text="{Binding TotalExposure, StringFormat={}{0:N0} 元}" HorizontalAlignment="Right" Foreground="DarkRed"/>
                </Grid>
                <Grid Margin="5">
                    <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions>
                    <TextBlock Text="▶ 總淨資產 (分母):" />
                    <TextBlock Grid.Column="1" Text="{Binding TotalCapital, StringFormat={}{0:N0} 元}" HorizontalAlignment="Right" Foreground="DarkGreen"/>
                </Grid>
                <Border Background="LightYellow" CornerRadius="5" Padding="10" Margin="0,15,0,0">
                    <StackPanel>
                        <TextBlock Text="💰 總資產槓桿倍數:" FontSize="16" HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding Leverage, StringFormat={}{0:F2} 倍}" FontSize="28" FontWeight="ExtraBold" HorizontalAlignment="Center" Margin="0,5,0,10"/>
                    </StackPanel>
                </Border>
                
                <Border Background="AliceBlue" CornerRadius="5" Padding="10" Margin="0,15,0,0">
                    <StackPanel>
                        <TextBlock Text="🚩 當前風險狀態:" FontSize="16" HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding RiskStatus}" FontSize="24" FontWeight="ExtraBold" HorizontalAlignment="Center" Margin="0,5,0,10" Foreground="Crimson"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </GroupBox>
    </Grid>
</Window>